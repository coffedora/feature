{
    "id": "prebuild",
    "version": "0.1.0",
    "name": "Fedora Codespace",
    "description": "Fedora Devcontainer designed for Cloud",
    "documentationURL": "https://github.com/d1sev/template/tree/main/src/init",
    "licenseURL": "https://github.com/d1sev/template/blob/main/LICENSE",
    "options": {
        "userName": {
            "type": "string",
            "description": "automatic use existing user with home dir or create remote-user",
            "default": "coffedora"
        },
        "copr": {
            "type": "string",
            "description": "Add Copr Repositories.automatic Recognizes ENV variables of packages to add their copr repos",
            "default": "automatic"
        },
        "dnfInstall": {
            "type": "string",
            "description": "packages installed by default separated by whitespaces. automatic installs requirements including those of other settings ",
            "default": "gh zsh fzf bat ripgrep fd fontconfig openssh-server"
        },
        "dnfRemove": {
            "type": "string",
            "description": "Remove packages from Image",
            "default": "automatic"
        },
        "languageSupport": {
            "type": "string",
            "description": "Install additional language support. Language Support is distributed in own scripts(Currently only for go)",
            "default": "go"
        }
    },
    "platforms": [
        "Fedora"
    ]
}